{% extends 'layouts/base.html.twig' %}

{% block title %} Portada {% endblock %}

{% block main %}
<div class="d-flex flex-col justify-content-center m-4 p-2">
    <h2 class="text-center">ÚLTIMAS PELÍCULAS AÑADIDAS CON CARÁTULAS</h2>
    <div class="d-flex justify-content-center">
        {% for peli in pelisLastsWithCovers %} 
            <div class="w-300 h-quarter m-2 p-2">
                <img src="{{ asset(covers_directory ~'/'~ (peli.caratula ?? 'default.jpg' ))}}" class="h-250 w-300 rounded-top" alt="{{ peli.titulo }}" title="{{ peli.titulo }}"> 
                <div class="col-sm text-center">{{ peli.titulo }}</div>
            </div>           
        {% endfor %}
    </div>
</div>

<div class="d-flex flex-col justify-content-center m-4 p-2">
    <h2 class="text-center">PELÍCULAS MEJOR VALORADAS</h2>
    <div class="d-flex justify-content-center">
        {% for peli in pelisMejorValoradas %} 
            <div class="w-200 h-quarter m-2 p-2">
                <img src="{{ asset(covers_directory ~'/'~ (peli.caratula ?? 'default.jpg' ))}}" class="h-250 w-300 rounded-top" alt="{{ peli.titulo }}" title="{{ peli.titulo }}"> 
                <div class="col-sm text-center">{{ peli.titulo }}</div>
                <div class="col-sm text-center font-weight-bold">VALORACIÓN: {{ peli.valoracion }}</div>
            </div>           
        {% endfor %}
    </div>
</div>

{# <div class="container-fluid">
  <div class="row">
        {% for peli in peliculas %}
        <!-- Card with no padding with a content container nested inside of it -->
        <div class="w-400 mw-full"> <!-- w-400 = width: 40rem (400px), mw-full = max-width: 100% -->
        <div class="card p-0 d-flex flex-col"> <!-- p-0 = padding: 0 -->
            <img src="{{ asset(covers_directory ~'/'~ (peli.caratula ?? 'default.jpg' ))}}" class="img-fluid rounded-top" alt="{{ peli.titulo }}" title="{{ peli.titulo }}">  
            <!-- Nested content container inside card -->
            <div class="content">
            <h2 class="content-title">
                {{ peli.titulo }}
            </h2>
            <p class="text-muted">
                Dir: {{ peli.director }}
            </p>
            <p class="text-muted">
                Duracion: {{ peli.duracion }}
            </p>
            <p class="text-muted">
                Género: {{ peli.genero }}
            </p>
            <div c
            <div class="text-center mt-6 alert alert-success"> <!-- text-right = text-align: right -->
                <a href="{{ path('movie_show', {'id':peli.id })}}" class="btn">Más detalles</a>
            </div>
            </div>
        </div>
        </div>
        {% endfor %}
    </div>
</div> #}

{% endblock %}
