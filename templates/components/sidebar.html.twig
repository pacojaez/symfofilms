{% block sidebar %}
    <!-- Sidebar (immediate child of the page wrapper) -->
    
      <div class="sidebar-menu">
        <!-- Sidebar brand -->
        <a href=" {{ path('home') }}" class="sidebar-brand">
          <img src="/img/icono.png" alt="SymfoFilms">
          Admin Panel
        </a>
        <!-- Sidebar content with the search box -->
        <div class="sidebar-content">
          <div class="d-flex flex-row m-2 p-2">
            <form methhod='POST' action="{{ path('users_search') }}">
              <input type="text" class="form-control m-2 p-2" placeholder="Buscador de usuarios" name="valor" />
              <input type='submit' class="btn btn-primary m-2" value="BUSCAR"/>
            </form>
          </div>
          {# <div class="mt-10 font-size-12"> <!-- mt-10 = margin-top: 1rem (10px), font-size-12 = font-size: 1.2rem (12px) -->
            Press <kbd>/</kbd> to focus
          </div> #}
        </div>
        <!-- Sidebar links and titles -->
         <div class="sidebar-content">
          <a href="{{ path('all_users') }}"><h5 class="sidebar-title">USUARIOS</h5></a>
          {# <div class="sidebar-content">
          <a href="{{ path('users_search') }}"><h5 class="sidebar-title">BUSCADOR DE USUARIOS</h5></a> #}
          <div class="sidebar-divider"></div>
          <a href="{{ path('admin_searchlogs') }}"><h5 class="sidebar-title">LOGS DE BUSQUEDA</h5></a>
          <div class="sidebar-divider"></div>
          <a href="{{ path('admin_usersactions') }}"><h5 class="sidebar-title">LAST USERS ACTIONS</h5></a>
          <div class="sidebar-divider"></div>
          <a href="{{ path('admin_docs') }}" class="sidebar-link">DOCS DEL PROYECTO</a>
          <div class="sidebar-divider"></div>
          <a href="{{ path('admin_todo') }}" class="sidebar-link">TODO</a>
           <div class="sidebar-divider"></div>
          <a href="{{ path('dummy_dql') }}" class="sidebar-link">DUMMY DQL</a>
          <div class="sidebar-divider"></div>
          <a href="{{ path('dummy_qb') }}" class="sidebar-link">DUMMY QUERYBUILDER</a>
        </div>
      <div class="sidebar-content">
      {# <h2 class="p-2 m-4 font-weight-bold">API DEL PROYECTO </h2> #}
      <details class="collapse-panel mw-full"> 
        <summary class="collapse-header font-weight-bold">
          API: MOVIES AND ACTORS
        </summary>
        <div class="collapse-content">
            <ul class="m-4">
                <li><a href="{{ path('api_allMovies', {'formato':'json'}) }}" class="sidebar-link">API MOVIES JSON</a></li>
                <li><a href="{{ path('api_allMovies', {'formato':'csv'}) }}" class="sidebar-link">API MOVIES CSV</a></li>
                <li><a href="{{ path('api_allMovies', {'formato':'xml'}) }}" class="sidebar-link">API MOVIES XML</a></li>      
                <li><a href="{{ path('api_allActors', {'formato':'json'}) }}" class="sidebar-link">API ACTORS JSON</a></li>
                <li><a href="{{ path('api_allActors', {'formato':'csv'}) }}" class="sidebar-link">API ACTORS CSV</a></li>
                <li><a href="{{ path('api_allActors', {'formato':'xml'}) }}" class="sidebar-link">API ACTORS XML</a></li>
            </ul>
        </div>
      </details>
    </div>     
        {# <a href="#" class="sidebar-link">CLI commands</a>
        <br />
        <h5 class="sidebar-title">Components</h5>
        <div class="sidebar-divider"></div>
        <a href="#" class="sidebar-link">File explorer</a>
        <a href="#" class="sidebar-link">Spreadsheet</a>
        <a href="#" class="sidebar-link">Map</a>
        <a href="#" class="sidebar-link">Messenger</a> #}
      </div>
    
{% endblock %}