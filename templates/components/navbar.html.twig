{% block navbar %}
	<!-- Navbar (immediate child of the page wrapper) -->
	<nav
		class="navbar justify-content-between">
		<!-- Navbar content (with toggle sidebar button) -->
		<div
			class="navbar-content">
			{% if is_granted("IS_AUTHENTICATED_FULLY") %}
				<nav class="navbar navbar-fixed-bottom">
					<button class="btn" type="button" onclick="halfmoon.toggleSidebar()">
						<i class="fa fa-bars" aria-hidden="true"></i>
					</button>
				</nav>
			{% endif %}
		  {# <button class="btn btn-action" type="button">
		            <i class="fa fa-bars" aria-hidden="true"></i>
		            <!-- Toggle sidebar -->
		            
		            {# <span class="sr-only">Toggle sidebar</span> <!-- sr-only = show only on screen readers -->
		          </button> #}
			{# <button class="btn" type="button" onclick="halfmoon.toggleSidebar()">Toggle sidebar</button> #}
		</div>
		<!-- Navbar brand -->
		<a href="{{ path('portada') }}" class="navbar-brand ml-0">
			<img src="/img/icono.png" alt="SymfoFilms">
			SymfoFilms <span class="navbar-text text-monospace d-none d-xl-block">v1.0</span>
		</a>
		<!-- Navbar text -->
		
		<!-- text-monospace = font-family shifted to monospace -->
		<!-- Navbar nav -->
		<ul class="navbar-nav d-none d-md-flex justify-content-end">
		<div class="dropdown m-2">
			<button class="btn" data-toggle="dropdown" type="button" id="dropdown-toggle-btn-1" aria-haspopup="true" aria-expanded="false">
				Peliculas <i class="fa fa-angle-down ml-5" aria-hidden="true"></i> <!-- ml-5 = margin-left: 0.5rem (5px) -->
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdown-toggle-btn-1">
				<h6 class="dropdown-header">LET´S ROCK</h6>
				<a href="{{ path('all_movies') }}" class="dropdown-item nav-link {{ app.request.get('_route') == 'all_movies' ? 'active'}}">TODAS LAS PELICULAS</a>
				<a href="{{ path('movie_search') }}" class="dropdown-item nav-link {{ app.request.get('_route') == 'movie_search' ? 'active'}}">BUSCAR PELÍCULA</a>
				{#<div class="dropdown-divider"></div>
				 <div class="dropdown-content">
					<button class="btn btn-block" type="button">Button</button>
				</div> #}
			</div>
		</div>
		<div class="dropdown m-2">
			<button class="btn" data-toggle="dropdown" type="button" id="dropdown-toggle-btn-1" aria-haspopup="true" aria-expanded="false">
				Actores <i class="fa fa-angle-down ml-5" aria-hidden="true"></i> <!-- ml-5 = margin-left: 0.5rem (5px) -->
			</button>
			<div class="dropdown-menu" aria-labelledby="dropdown-toggle-btn-1">
				<h6 class="dropdown-header">LET´S ROLL</h6>
				<a href="{{ path('all_actors') }}" class="dropdown-item nav-link {{ app.request.get('_route') == 'all_actors' ? 'active'}}">TODOS LOS ACTORES/TRICES</a>
				<a href="{{ path('actor_search') }}" class="dropdown-item nav-link {{ app.request.get('_route') == 'actor_search' ? 'active'}}">BUSCAR ACTOR/TRIZ</a>
				{#<div class="dropdown-divider"></div>
				 <div class="dropdown-content">
					<button class="btn btn-block" type="button">Button</button>
				</div> #}
			</div>
		</div>
			{# <li class="nav-item {{ app.request.get('_route') == 'all_movies' ? 'active'}}"><a href="{{ path('all_movies') }}" class="nav-link">Peliculas</a></li> #}
			{# <li class="nav-item {{ app.request.get('_route') == 'all_actors' ? 'active'}}"><a href="{{ path('all_actors') }}" class="nav-link">Actores</a></li> #}
			<li class="nav-item {{ app.request.get('_route') == 'contact' ? 'active'}}"><a href="{{ path('contact') }}" class="nav-link">Contacto</a></li>
		</ul>
		<!-- Toggling dark mode -->
    <button class="mx-2 btn btn-primary" type="button" onclick="toggleDemo()">
      <i class="fa fa-moon-o"></i>
    </button>

	<!-- Navbar content (with the dropdown menu) -->
		<div
			class="ml-auto navbar-content d-md-none">
			<!-- d-md-none = display: none on medium screens and up (width > 768px), ml-auto = margin-left: auto -->
			<div class="dropdown with-arrow">
				<button class="btn" data-toggle="dropdown" type="button" id="navbar-dropdown-toggle-btn-1">
					Menu
					<i class="fa fa-angle-down" aria-hidden="true"></i>
				</button>
				<div
					class="dropdown-menu dropdown-menu-right w-200" aria-labelledby="navbar-dropdown-toggle-btn-1">
					<!-- w-200 = width: 20rem (200px) -->
					<a href="{{ path('all_movies') }}" class="dropdown-item">PELÍCULAS</a>
					<a href="{{ path('all_actors') }}" class="dropdown-item">ACTORES</a>
					<a href="{{ path('contact') }}" class="dropdown-item">CONTACTO</a>
					<div class="dropdown-divider"></div>
					{# <div class="dropdown-content">
						<form action="..." method="...">
							<div class="form-group">
								<input type="text" class="form-control" placeholder="Email address" required="required">
							</div>
							<button class="btn btn-primary btn-block" type="submit">Sign up</button>
						</form>
					</div> #}
				</div>
			</div>
		</div>
    {# <button class="mx-2 btn btn-primary" type="button" onclick="toggleDemo()">
      <i class="fa fa-sun-o" id="sun"></i>
    </button> #}
	<span class="navbar-text text-monospace d-none d-md-flex">{{ "now"|date('d/m/Y H:i', timezone="Europe/Madrid")}}</span>
	<div class="d-inline-flex align-items-center">
		{% if app.user %}
			<div class="dropdown with-arrow">
				<div class="d-inline-flex align-items-center">
					<p class="p-2 m-2">Hola {{ app.user.displayname }}</p>
					<button class="btn" data-toggle="dropdown" type="button" id="navbar-dropdown-toggle-btn-1">
						
						<i class="fa fa-angle-down" aria-hidden="true"></i>
					</button>
				</div>
				<div
					class="dropdown-menu dropdown-menu-center w-200" aria-labelledby="navbar-dropdown-toggle-btn-1">
					<!-- w-200 = width: 20rem (200px) -->
					<a href="{{ path('home') }}" class="dropdown-item text-center">HOME</a>
					<div class="dropdown-divider"></div>
					<div class="dropdown-item text-center">
						<a href="{{ path('app_logout') }}" class="btn btn-danger" role="button">Sign out</a>
					</div>		
					<div class="dropdown-divider"></div>
				</div>
			</div>
		{% else %}
			<div class="m-2 p-2">
				<a href="{{ path('app_login') }}" class="btn btn-primary" role="button">Sign in</a>
			</div>
			<div class="m-2 p-2">
				<a href="{{ path('app_register') }}" class="btn btn-primary" role="button">Register</a>
			</div>
		{% endif %}
	</div>
		<!-- Navbar form (inline form) -->
		{# <form class="ml-auto form-inline d-none d-md-flex" action="{{ path('movie_search') }}" method="POST">
			<input type="text" class="form-control" name="valor" id="valor" placeholder="Buscador de películas" required="required"> <button class="btn btn-primary" type="submit">SEARCH</button>
		</form> #}
		<!-- Navbar content (with the dropdown menu) -->
		{# <div
			class="ml-auto navbar-content d-md-none">
			<!-- d-md-none = display: none on medium screens and up (width > 768px), ml-auto = margin-left: auto -->
			<div class="dropdown with-arrow">
				<button class="btn" data-toggle="dropdown" type="button" id="navbar-dropdown-toggle-btn-1">
					Menu
					<i class="fa fa-angle-down" aria-hidden="true"></i>
				</button>
				<div
					class="dropdown-menu dropdown-menu-right w-200" aria-labelledby="navbar-dropdown-toggle-btn-1">
					<!-- w-200 = width: 20rem (200px) -->
					<a href="{{ path('all_movies') }}" class="dropdown-item">PELÍCULAS</a>
					<a href="{{ path('all_actors') }}" class="dropdown-item">ACTORES</a>
					<a href="{{ path('contact') }}" class="dropdown-item">CONTACTO</a>
					<div class="dropdown-divider"></div>
					{# <div class="dropdown-content">
						<form action="..." method="...">
							<div class="form-group">
								<input type="text" class="form-control" placeholder="Email address" required="required">
							</div>
							<button class="btn btn-primary btn-block" type="submit">Sign up</button>
						</form>
					</div> 
				</div>
			</div>
		</div> #}
	</nav>
{% endblock %}
