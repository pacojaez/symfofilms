<div class="example-wrapper">
        <div class="">
            {{ form_start(formulario)}}
                <div class="d-flex flex-row justify-content-around align-items-center">
                    <div class="form-group">{{ form_row(formulario.content)}}</div>
                    <div class="form-group">{{ form_row(formulario.peli_id,  {
                                        'attr': {'value': peli.id }
                                        })
                                }}
                    </div>
                    <div class="form-group">{{ form_row(formulario.comentar)}}</div>
                    {{ form_rest(formulario)}}
                </div>
            {{ form_end(formulario) }}
        </div>
        <h4 class="text-center m-2 p-2 font-weight-bold">RESULTADOS</h4>
        <div class="d-flex flex-row justify-content-center">
            <a href="{{ path('all_movies') }}">VOLVER AL LISTADO</a>
        </div>
    </div>