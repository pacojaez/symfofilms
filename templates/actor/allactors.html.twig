{% extends 'layouts/base.html.twig' %}

{% block title %}Todos los Actores{% endblock %}

{% block main %}
{# <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style> #}
<div class="w-full">
<div class="flex-row p-2 m-2 d-flex justify-content-around align-items-center">
    {# Crear variable en Twig #}
    {% set ruta = 'all_actors' %}
         {% if is_granted('create', entityFaker.getMock('Actor') ) %}
            <a href=" {{ path('actor_create')}}"><button class="btn btn-primary">AÑADIR NUEVO ACTOR</button></a>
        {% endif %}
        {% if totalPaginas > 1 %}
            {% include('components/pagination.html.twig') %}
        {% endif %}	
</div>

  {% include 'actor/includes/actors_list.html.twig' %}


		
{% endblock %}
